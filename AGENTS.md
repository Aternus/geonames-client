# geonames-client project

## Running PHP

1. Run PHP by using Docker. For examples see `run-prepush.sh`.
2. ALWAYS use the `php7` service. Using a different service will affect the
   generated `vendor` folder.
